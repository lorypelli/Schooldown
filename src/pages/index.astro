---
import axios from 'axios';
const res = await axios.get(`http://localhost:8080/getData`);
return Astro.redirect(`/${Object.keys(res.data)[Math.floor(Math.random() * (21 - 1 + 1) + 1)].replaceAll(' ', '-')}`);
---
