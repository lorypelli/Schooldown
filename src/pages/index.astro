---
import axios from 'axios';
const res = await axios.get(`https://schooldown-astro.vercel.app/api/getData/`);
const nomeRegione = Object.keys(res.data);
let randomIndex = Math.floor(Math.random() * (21 - 1 + 1) + 1);
return Astro.redirect(`/${nomeRegione[randomIndex].replaceAll(' ', '-')}`);
---
